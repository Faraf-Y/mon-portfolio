# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

**MonPortfolio** — static portfolio website (HTML, CSS, vanilla JavaScript). No framework, no build tool. Open `index.html` directly in a browser.

## Architecture

Single-page site. All styles live in a `<style>` block inside `index.html`, or in a separate `style.css` file if extracted. No dependencies to install.

- Dark background (`#0f0f0f`), minimalist and professional design
- Mobile-first layout
- Font: Inter (Google Fonts)

## Conventions

- **Code** (variables, functions, CSS classes, comments): English
- **Visible content** (text, labels, UI copy): French
